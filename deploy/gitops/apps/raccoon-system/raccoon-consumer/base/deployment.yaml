---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: raccoon-consumer
  namespace: raccoon-system
  labels:
    app.kubernetes.io/name: raccoon-consumer
    app.kubernetes.io/component: consumer
    app.kubernetes.io/part-of: market-raccoon
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: raccoon-consumer
  template:
    metadata:
      labels:
        app: raccoon-consumer
        app.kubernetes.io/name: raccoon-consumer
        app.kubernetes.io/component: consumer
        app.kubernetes.io/part-of: market-raccoon
    spec:
      terminationGracePeriodSeconds: 45
      containers:
        - name: app
          image: ghcr.io/market-raccoon/raccoon-consumer:latest
          command: ["-config", "/etc/market-raccoon/consumer.jsonc"]
          ports:
            - name: service
              containerPort: 8081
              protocol: TCP
          env:
            - name: SHARD_INDEX
              value: "0"
            - name: SHARD_COUNT
              value: "1"
          envFrom:
            - secretRef:
                name: raccoon-app-secrets
          volumeMounts:
            - name: config
              mountPath: /etc/market-raccoon
              readOnly: true
          startupProbe:
            httpGet:
              path: /readyz
              port: 8081
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 15
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: config
          configMap:
            name: raccoon-consumer-config
