---
apiVersion: batch/v1
kind: Job
metadata:
  name: stream-analytics-migrate
  namespace: stream-analytics-system
  labels:
    app.kubernetes.io/name: stream-analytics-migrate
    app.kubernetes.io/component: migrate
    app.kubernetes.io/part-of: stream-analytics
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: stream-analytics-migrate
        app.kubernetes.io/name: stream-analytics-migrate
        app.kubernetes.io/component: migrate
        app.kubernetes.io/part-of: stream-analytics
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: ghcr.io/stream-analytics/stream-analytics-migrate:latest
          command:
            - /bin/sh
            - -c
            - >-
              /usr/local/bin/migrate -target timescale up &&
              /usr/local/bin/migrate -target clickhouse up
          envFrom:
            - secretRef:
                name: stream-analytics-app-secrets
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
