# S5 Evidence — D1, D2, D3

## Commits

```
b94f0b5 docs(operations): add degradation contract (S5-D1)
babebee feat(observability): close store gaps in SLO (S5-D2)
3be7e9d test(store): soak test for storage-slow scenario (S5-D3)
```

## Gates Passed (per commit)

All 6 gates passed for each commit:
- `make registry-check` — passed
- `make invariants-check` — passed (domain protobuf-free + determinism guards)
- `make test-workspace` — passed (all modules)
- `make test-workspace-race` — passed (all modules with -race)
- `make test-replay-golden` — passed
- `make lint` — passed (0 issues)

## D1 — Degradation Contract

File: `docs/operations/degradation.md`

Content:
- 3 degradation scenarios (CH slow, CH down, CH recovering)
- Propagation chain for each scenario
- 4 PromQL queries with interpretation
- Mitigation actions table
- Alert reference table

## D2 — Observability Gaps Closed

### Recording Rules Updated
- `slo:data_loss:cold_commit_errors_rate_5m` now includes:
  - `store_commit_total{status="failed"}`
  - `store_flush_total{status="failed"}`
- Same addition propagated to 30m, 1h, 6h variants

### New Alert
- `StoreCommitLatencyHigh`: p95 > 1s sustained 5m, severity=ticket
- Runbook: `docs/operations/degradation.md`

### Dashboard Updated
- New "Store — Backpressure" row in store.json
- Panel: "Consumer Lag vs Commit Latency" (dual-axis)
- Panel: "Redelivery Rate"

## D3 — Storage-Slow Soak Test

### Interface Extraction
- `StoreWriter` interface extracted from `StoreBatcher` (batcher.go)
- `clickhouse.Writer` satisfies it implicitly — no other changes needed
- All existing tests pass unchanged

### Soak Test
- `TestStoreSoak_StorageSlow` in `cmd/store/soak_test.go`
- `slowWriter` wrapper injects 10ms latency before delegating
- 200 unique envelopes + 3x replay = 800 total messages
- Asserts:
  1. Exactly 200 commits (zero duplicates from redelivery)
  2. Consumed counter = 800
  3. p50 latency >= 10ms (proves slow writer effective)
  4. p95 < 50ms (no unexpected overhead)
- Skips in `-short` mode

### Test Output
```
=== RUN   TestStoreSoak_StorageSlow
    soak_test.go:365: total processed=800 unique=200 replays=3 commits=200
    soak_test.go:383: latency p50=11.023458ms p95=11.048417ms max=11.245958ms (injected=10ms)
--- PASS: TestStoreSoak_StorageSlow (8.80s)
```
