---
apiVersion: v1
kind: Service
metadata:
  name: stream-analytics-store
  namespace: stream-analytics-system
  labels:
    app.kubernetes.io/name: stream-analytics-store
    app.kubernetes.io/component: store
    app.kubernetes.io/part-of: stream-analytics
    app.kubernetes.io/managed-by: argocd
spec:
  selector:
    app: stream-analytics-store
  ports:
    - port: 80
      name: service
      targetPort: service
      protocol: TCP
