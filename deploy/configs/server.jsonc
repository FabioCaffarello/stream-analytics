{
  // Runtime config mounted in container at /etc/market-raccoon/server.jsonc
  // The server binary uses an in-memory bus (hardcoded); no JetStream consumer.
  "bus": {
    "type": "inmemory"
  },
  "log": {
    "level": "info",
    "format": "text"
  },
  "http": {
    "addr": ":8080",
    "read_timeout": "10s",
    "write_timeout": "15s",
    "idle_timeout": "60s",
    "shutdown_timeout": "10s"
  }
}
