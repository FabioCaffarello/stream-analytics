---
apiVersion: v1
kind: Service
metadata:
  name: raccoon-store
  namespace: raccoon-system
  labels:
    app.kubernetes.io/name: raccoon-store
    app.kubernetes.io/component: store
    app.kubernetes.io/part-of: market-raccoon
    app.kubernetes.io/managed-by: argocd
spec:
  selector:
    app: raccoon-store
  ports:
    - port: 80
      name: service
      targetPort: service
      protocol: TCP
