# Allocation Budget Registry — Market Raccoon
#
# Format: BenchmarkName<tab>MaxAllocs
# Lines starting with # are comments; blank lines are ignored.
#
# Budgets set ~15-20% above measured baseline (2026-02-20, Apple M4)
# to allow natural variance while catching regressions.
#
# Target: <10 allocs per single domain event on core hot paths.
#
# Per-event interpretation:
#   HashFieldsFast5:     0 allocs/call  (primitive, zero-alloc FNV-1a)
#   DecodePayload:      10 allocs/decode (called once per inbound event)
#   PolicyKit:           6 allocs/decision (called per overload check)
#   Ingest_1000:       ~18 allocs/event (batch: encode + bus publish)
#   ApplyDelta_1000:   <1 alloc/level  (per-1000-level batch)
#   E2E pipelines:    48-55 allocs/op  (multi-step, acceptable)
#   DeliveryFanOut:    ~3 allocs/session/event (scales linearly)
#   SessionWriteProto:   7 allocs/write (meets <10 target!)
#   SessionWriteJSON:   24 allocs/write (JSON overhead, acceptable)

# --- Shared primitives (hot-path) ---
BenchmarkHotPathHashFieldsFast5	1
BenchmarkHotPathHashFields5	7
BenchmarkHotPathDecodePayloadAndSubjectFromEnvelope	12
BenchmarkHotPathPolicyKitApplySingle	8
BenchmarkHotPathPolicyKitApplyBatchLen1	8

# --- Domain operations ---
BenchmarkApplyDelta_1000Levels	20

# --- E2E pipelines (multi-step, higher budget) ---
BenchmarkE2E_IngestToOrderbookSnapshot	55
BenchmarkE2E_TradeToCandle	65
BenchmarkE2E_MarkPriceToStats	65

# --- Ingest (batch of 1000) ---
BenchmarkIngest_1000Envelopes	20000

# --- Delivery ---
BenchmarkDeliveryFanOut_50Sessions	175
BenchmarkDeliveryFanOut_200Sessions	700
BenchmarkSessionWriteJSON	28
BenchmarkSessionWriteProto	10
