---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stream-analytics-server
  namespace: stream-analytics-system
  labels:
    app.kubernetes.io/name: stream-analytics-server
    app.kubernetes.io/component: server
    app.kubernetes.io/part-of: stream-analytics
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stream-analytics-server
  template:
    metadata:
      labels:
        app: stream-analytics-server
        app.kubernetes.io/name: stream-analytics-server
        app.kubernetes.io/component: server
        app.kubernetes.io/part-of: stream-analytics
    spec:
      terminationGracePeriodSeconds: 45
      containers:
        - name: app
          image: ghcr.io/stream-analytics/stream-analytics-server:latest
          command: ["-config", "/etc/stream-analytics/server.jsonc"]
          ports:
            - name: service
              containerPort: 8080
              protocol: TCP
          envFrom:
            - secretRef:
                name: stream-analytics-app-secrets
          volumeMounts:
            - name: config
              mountPath: /etc/stream-analytics
              readOnly: true
          startupProbe:
            httpGet:
              path: /readyz
              port: 8080
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: config
          configMap:
            name: stream-analytics-server-config
